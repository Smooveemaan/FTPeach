# nginx_davext: nginx core dav + dav_ext over HTTP. nginx insists on a trailing
# slash for MKCOL and for MOVE/COPY/DELETE of collections, and uploads above
# client_max_body_size (8 MiB) get 413. small/ is a 16 MiB tmpfs.
dav_ext_lock_zone zone=davlock:10m;

server {
    listen 80;
    server_name _;
    root /srv/dav;
    client_max_body_size 8m;
    client_body_temp_path /tmp/nginx-body;

    location / {
        auth_basic "FTPeach matrix";
        auth_basic_user_file /etc/nginx/dav.htpasswd;
        dav_methods PUT DELETE MKCOL COPY MOVE;
        dav_ext_methods PROPFIND OPTIONS LOCK UNLOCK;
        dav_ext_lock zone=davlock;
        dav_access user:rw group:r all:r;
        create_full_put_path on;
        autoindex on;
    }
}
