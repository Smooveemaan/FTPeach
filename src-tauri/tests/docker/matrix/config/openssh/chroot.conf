# openssh_chroot: SFTP-only account jailed to /srv/chroot with a pre-login
# banner. Inside the jail: home/ (writable fixtures), readonly/ (root-owned) and
# small/ (a 16 MiB tmpfs for disk-full tests).
Port 22
PermitRootLogin no
UsePAM yes
AuthorizedKeysFile /etc/ssh/authorized_keys/%u
UseDNS no
PrintMotd no
LogLevel VERBOSE
# Every test connection arrives from the same Docker gateway address, and
# wrong-password or failed-negotiation tests are deliberate: without these,
# OpenSSH 10 penalises and throttles that address for the rest of the run.
PerSourcePenalties no
MaxStartups 100:30:200
HostKey /etc/ssh/ssh_host_ed25519_key
HostKey /etc/ssh/ssh_host_ecdsa_key
HostKey /etc/ssh/ssh_host_rsa_key
PubkeyAuthentication yes
PasswordAuthentication yes
KbdInteractiveAuthentication no
Banner /etc/ssh/banner.txt
Subsystem sftp internal-sftp

Match User testuser
    ChrootDirectory /srv/chroot
    ForceCommand internal-sftp -d /home
    AllowTcpForwarding no
    X11Forwarding no
