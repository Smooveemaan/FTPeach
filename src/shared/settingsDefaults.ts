// Shared with src-tauri/src/store/settings.rs through settingsDefaults.json. Runtime
// shape/range normalization remains in features/settings/useSettings.ts.
import defaults from './settingsDefaults.json' with { type: 'json' };

export default defaults;
